
sudo docker build -t fernavat/graphql-server .
sudo docker run -p 4000:4000 --name graphql-server -d fernavat/graphql-server
sudo docker stop graphql-server
sudo docker rm graphql-server


// create-start-stop-remove graphql server
sudo docker-compose up -d //with --build to rebuild Dockerfile
sudo docker-compose start
sudo docker-compose stop
sudo docker-compose down //with --volumes to also remove data volume

// run prisma deploy
sudo docker-compose up -d prisma
prisma deploy
sudo docker-compose down